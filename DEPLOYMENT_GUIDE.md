# 🚀 面向海外用户的云服务器部署指南

## 📋 目录
- [部署架构建议](#部署架构建议)
- [云服务商推荐](#云服务商推荐)
- [部署方案详解](#部署方案详解)
- [详细部署步骤](#详细部署步骤)
- [成本估算](#成本估算)

---

## 🏗️ 部署架构建议

### 推荐架构（面向全球用户）

```
┌─────────────┐
│   Cloudflare  │  ← CDN + DDoS 防护
│   (全球加速)   │
└──────┬──────┘
       │
┌──────┴────────────────────────────┐
│  前端静态资源 (Vercel/Netlify)      │  ← React 应用
└────────────────────────────────────┘
       │
┌──────┴────────────────────────────┐
│  后端 API 服务器 (AWS/Google Cloud) │  ← Node.js + Express
│  区域：美国东部/欧洲/亚洲            │
└──────┬────────────────────────────┘
       │
┌──────┴────────────────────────────┐
│  数据库 (云数据库服务)               │  ← MySQL (RDS/Cloud SQL)
└────────────────────────────────────┘
```

---

## 🌍 云服务商推荐

### 方案一：国外云服务商（推荐用于主要服务外国用户）

#### 1. **Vercel + AWS / Google Cloud** ⭐⭐⭐⭐⭐

**优势：**
- ✅ **Vercel** 部署前端：零配置、全球 CDN、自动 HTTPS
- ✅ **AWS/GCP** 部署后端：稳定、全球节点、成熟生态
- ✅ 外国用户访问速度快（服务器在欧美）
- ✅ 支付方便（Stripe、PayPal 等）
- ✅ 无备案要求

**适用场景：**
- 主要服务欧美用户
- 需要快速上线
- 预算充足（$50-200/月）

**推荐配置：**
- 前端：Vercel（免费版即可，或 Pro $20/月）
- 后端：AWS EC2 t3.medium（2核4GB）$30/月 或 GCP e2-medium $25/月
- 数据库：AWS RDS MySQL db.t3.micro $15/月 或 Cloud SQL $15/月

---

#### 2. **DigitalOcean + Cloudflare** ⭐⭐⭐⭐

**优势：**
- ✅ 价格透明，性价比高
- ✅ 简单易用，适合中小团队
- ✅ Cloudflare 免费 CDN + DDoS 防护
- ✅ 全球多个数据中心

**适用场景：**
- 预算有限（$20-100/月）
- 需要快速部署
- 中等流量应用

**推荐配置：**
- 服务器：DigitalOcean Droplet (2GB RAM) $12/月
- 数据库：DigitalOcean Managed MySQL $15/月
- CDN：Cloudflare（免费）

---

#### 3. **Railway / Render** ⭐⭐⭐⭐⭐

**优势：**
- ✅ **最简单**：GitHub 推送自动部署
- ✅ 内置数据库、Redis 等服务
- ✅ 按使用量付费，起步低
- ✅ 自动 HTTPS 和域名

**适用场景：**
- 快速原型和 MVP
- 小型团队
- 需要快速迭代

**推荐配置：**
- Railway：$5/月起（按使用量）
- Render：免费版起步，付费版 $7/月起

---

### 方案二：国内云服务商 + 海外加速

#### 4. **阿里云 / 腾讯云（香港/新加坡节点）** ⭐⭐⭐

**优势：**
- ✅ 价格相对便宜
- ✅ 中文支持好
- ✅ 适合同时服务国内外用户

**劣势：**
- ⚠️ 需要备案（如果用国内节点）
- ⚠️ 外国用户访问可能较慢
- ⚠️ 海外节点成本较高

**推荐配置：**
- 服务器：香港轻量应用服务器（2核2GB）$30-50/月
- 数据库：云数据库 MySQL（香港）$20-40/月
- CDN：阿里云全球加速

---

## 📦 部署方案详解

### 方案 A：全栈部署到单一服务器（适合起步）

**架构：**
```
[单台云服务器]
├── Nginx（反向代理 + 静态文件）
├── PM2（Node.js 进程管理）
├── MySQL（数据库）
└── 前端构建文件（dist）
```

**优点：**
- 成本低（$10-30/月）
- 部署简单
- 适合 MVP

**缺点：**
- 扩展性差
- 单点故障风险

---

### 方案 B：前后端分离 + 云数据库（推荐）

**架构：**
```
[前端] → Vercel/Netlify（CDN）
   ↓
[后端 API] → AWS EC2 / Railway
   ↓
[数据库] → AWS RDS / Railway Postgres
```

**优点：**
- 性能好（前端 CDN 加速）
- 扩展灵活
- 运维方便

**缺点：**
- 成本稍高（$30-100/月）
- 需要配置跨域

---

### 方案 C：容器化部署（适合生产环境）

**架构：**
```
Docker + Docker Compose
├── Nginx（容器）
├── Node.js API（容器）
├── MySQL（容器或云服务）
└── 前端静态文件（Nginx 容器）
```

**优点：**
- 易于扩展
- 环境一致
- 支持 Kubernetes

**缺点：**
- 配置复杂
- 需要学习 Docker

---

## 🛠️ 详细部署步骤

### 方案 1：Railway 一键部署（最简单）

#### 步骤 1：准备工作

1. **注册 Railway 账号**
   - 访问：https://railway.app/
   - 使用 GitHub 账号登录（推荐）

2. **准备代码仓库**
   - 将代码推送到 GitHub
   - 确保有 `.env.example` 文件

#### 步骤 2：部署后端

1. 在 Railway 创建新项目
2. 选择 "Deploy from GitHub repo"
3. 选择你的仓库
4. 配置环境变量：
   ```env
   PORT=5000
   NODE_ENV=production
   DB_HOST=xxxxx.railway.app
   DB_PORT=3306
   DB_NAME=railway
   DB_USER=root
   DB_PASSWORD=xxxxx
   JWT_SECRET=你的强密钥
   ```
5. Railway 自动检测 `package.json` 并部署
6. 添加 MySQL 数据库插件
7. 等待部署完成

#### 步骤 3：部署前端到 Vercel

1. **构建前端：**
   ```bash
   cd client
   npm run build
   ```

2. **部署到 Vercel：**
   - 访问：https://vercel.com/
   - 使用 GitHub 登录
   - 导入项目（选择 `client` 目录）
   - 配置环境变量：
     ```
     VITE_API_URL=https://你的后端域名.railway.app
     ```
   - 自动部署完成

#### 步骤 4：配置 CORS

在 `server/index.js` 中确保 CORS 配置：
```javascript
app.use(cors({
  origin: ['https://你的前端域名.vercel.app'],
  credentials: true
}));
```

---

### 方案 2：AWS / Google Cloud 部署

#### 步骤 1：创建 EC2 实例

1. **登录 AWS 控制台**
2. **启动 EC2 实例：**
   - 选择：Ubuntu 22.04 LTS
   - 实例类型：t3.medium（2核4GB）
   - 配置安全组：开放端口 80, 443, 22
   - 分配弹性 IP（重要！）

#### 步骤 2：服务器初始化

```bash
# 连接服务器
ssh -i your-key.pem ubuntu@your-ip

# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装 Node.js
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 安装 MySQL
sudo apt install -y mysql-server
sudo mysql_secure_installation

# 安装 Nginx
sudo apt install -y nginx

# 安装 PM2
sudo npm install -g pm2
```

#### 步骤 3：部署应用

```bash
# 克隆代码
git clone https://github.com/your-username/tour.git
cd tour

# 安装依赖
npm install
cd client && npm install && npm run build && cd ..

# 配置环境变量
nano .env
# 填入生产环境配置

# 初始化数据库
npm run seed

# 启动应用（使用 PM2）
pm2 start npm --name "tour-api" -- start
pm2 save
pm2 startup
```

#### 步骤 4：配置 Nginx

```bash
sudo nano /etc/nginx/sites-available/tour
```

配置内容：
```nginx
server {
    listen 80;
    server_name your-domain.com;

    # 前端静态文件
    location / {
        root /home/ubuntu/tour/client/dist;
        try_files $uri $uri/ /index.html;
    }

    # API 代理
    location /api {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

启用配置：
```bash
sudo ln -s /etc/nginx/sites-available/tour /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### 步骤 5：配置 SSL（Let's Encrypt）

```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

---

### 方案 3：Docker 部署

#### 创建 Dockerfile

**根目录 Dockerfile：**
```dockerfile
FROM node:18-alpine

WORKDIR /app

# 复制依赖文件
COPY package*.json ./
RUN npm install

# 复制代码
COPY . .

# 构建前端
WORKDIR /app/client
RUN npm install && npm run build

# 回到根目录
WORKDIR /app

# 暴露端口
EXPOSE 5000

# 启动命令
CMD ["npm", "start"]
```

**docker-compose.yml：**
```yaml
version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - DB_HOST=db
      - DB_PORT=3306
      - DB_NAME=shanghai_tour
      - DB_USER=root
      - DB_PASSWORD=${DB_PASSWORD}
    depends_on:
      - db
    volumes:
      - ./client/dist:/app/client/dist

  db:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
      - MYSQL_DATABASE=shanghai_tour
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"

volumes:
  mysql_data:
```

**部署：**
```bash
docker-compose up -d
```

---

## 💰 成本估算

### 小型应用（初期，< 1000 用户/天）

| 服务商 | 前端 | 后端 | 数据库 | 总计/月 |
|--------|------|------|--------|---------|
| **Railway** | 包含 | $5 | $5 | **$10-20** |
| **Vercel + DigitalOcean** | 免费 | $12 | $15 | **$27** |
| **Render** | 免费 | $7 | $0 | **$7-15** |
| **AWS** | S3+CF ($1) | t3.micro ($8) | RDS ($15) | **$24** |

### 中型应用（1000-10000 用户/天）

| 服务商 | 前端 | 后端 | 数据库 | CDN | 总计/月 |
|--------|------|------|--------|-----|---------|
| **Vercel + AWS** | $20 | $30 | $30 | 免费 | **$80** |
| **DigitalOcean** | 免费 | $24 | $15 | Cloudflare免费 | **$39** |
| **Google Cloud** | $10 | $25 | $20 | $10 | **$65** |

---

## ✅ 最终推荐

### 对于外国用户为主的场景：

1. **最推荐：Railway（后端）+ Vercel（前端）**
   - 成本：$10-30/月
   - 部署时间：30分钟
   - 适合：快速上线、中小型应用

2. **生产环境：AWS（全栈）**
   - 成本：$50-150/月
   - 稳定性：⭐⭐⭐⭐⭐
   - 适合：正式产品、大流量

3. **预算有限：DigitalOcean + Cloudflare**
   - 成本：$20-40/月
   - 性价比：⭐⭐⭐⭐
   - 适合：个人项目、初创公司

---

## 📝 部署后检查清单

- [ ] 域名已配置并解析
- [ ] SSL 证书已安装（HTTPS）
- [ ] 数据库已初始化
- [ ] 环境变量已正确配置
- [ ] CORS 配置正确
- [ ] 前端 API 地址指向生产环境
- [ ] 日志和监控已配置
- [ ] 备份策略已设置
- [ ] 性能测试通过
- [ ] 安全扫描通过

---

## 🔗 有用链接

- **Railway**: https://railway.app/
- **Vercel**: https://vercel.com/
- **Render**: https://render.com/
- **DigitalOcean**: https://www.digitalocean.com/
- **AWS**: https://aws.amazon.com/
- **Google Cloud**: https://cloud.google.com/
- **Cloudflare**: https://www.cloudflare.com/

---

## 📞 需要帮助？

部署过程中遇到问题，可以：
1. 查看各平台的官方文档
2. 检查应用日志：`pm2 logs` 或 Railway/Render 的日志面板
3. 确保数据库连接正常
4. 检查防火墙和安全组设置
